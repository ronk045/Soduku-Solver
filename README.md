# Soduku-Solver
Implemented a Sudoku solver in C using recursive backtracking. Built helper functions to validate row, column, and 3×3 zone constraints, then applied depth-first search with recursion to fill empty cells and solve valid 9×9 Sudoku puzzles efficiently.
